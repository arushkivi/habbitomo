{
  "name": "productivity-app",
  "version": "1.0.0",
  "description": "Habit Tracker, Pomodoro Clock, and Task Manager",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "cap:init": "npm install @capacitor/core @capacitor/cli && npx cap init",
    "cap:add:android": "npm install @capacitor/android && npx cap add android",
    "cap:add:ios": "npm install @capacitor/ios && npx cap add ios",
    "cap:add:electron": "npm install @capacitor-community/electron && npx cap add @capacitor-community/electron",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "cap:open:ios": "npx cap open ios",
    "cap:build:android": "npx cap sync android && cd android && ./gradlew assembleRelease",
    "cap:build:ios": "npx cap sync ios"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "keywords": [
    "habit-tracker",
    "pomodoro",
    "task-manager"
  ],
  "author": "",
  "license": "MIT"
}
